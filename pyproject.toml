[project]
name = "platform-e2e"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = [
    "pytest>=8.2.0",
    "httpx>=0.27.0",
    "fastapi>=0.111.0",
]

[tool.pytest.ini_options]
testpaths = ["tests/e2e"]
pythonpath = ["services/onboarding-api"]
